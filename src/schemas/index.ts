import { blockContent } from './content';
import { lab } from './lab';
import { post } from './post';
import { project } from './project';

export const schemaTypes = [blockContent, post, lab, project];
